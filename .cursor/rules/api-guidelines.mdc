---
description: REST API 설계 및 개발 가이드라인
ruleType: auto_attached
globs: ['src/modules/v1/**/*.controller.ts', 'src/router/**/*.ts']
---

# API 개발 가이드라인

## REST API 설계 원칙

### URL 명명 규칙

- **리소스는 복수형 명사**: `/users`, `/posts`, `/comments`
- **케밥-케이스 사용**: `/user-profiles`, `/api-keys`
- **버전 관리**: `/v1/users`, `/v2/users`
- **중첩 리소스**: `/users/:id/posts`

### HTTP 메서드 사용

- **GET**: 데이터 조회 (멱등성)
- **POST**: 데이터 생성
- **PUT**: 전체 데이터 수정 (멱등성)
- **PATCH**: 부분 데이터 수정
- **DELETE**: 데이터 삭제 (멱등성)

### 응답 형식 표준화

```typescript
// 성공 응답
{
  "success": true,
  "data": { ... },
  "message": "작업이 완료되었습니다"
}

// 실패 응답
{
  "success": false,
  "error": {
    "code": "USER_NOT_FOUND",
    "message": "사용자를 찾을 수 없습니다"
  }
}

// 페이지네이션
{
  "success": true,
  "data": [...],
  "pagination": {
    "page": 1,
    "limit": 10,
    "total": 100,
    "totalPages": 10
  }
}
```

### 상태 코드 가이드라인

- **200**: 성공
- **201**: 생성 성공
- **400**: 잘못된 요청
- **401**: 인증 필요
- **403**: 권한 없음
- **404**: 리소스 없음
- **422**: 유효성 검사 실패
- **500**: 서버 오류

### DTO 검증 규칙

- 모든 입력 데이터는 DTO로 검증
- `class-validator` 데코레이터 활용
- 적절한 에러 메시지 제공
