---
description: TypeScript 코딩 스타일 및 타입 안전성 가이드라인
ruleType: auto_attached
globs: ['src/**/*.ts']
---

# TypeScript 코딩 규칙

## 타입 정의 원칙

### 명시적 타입 선언

```typescript
// Good: 명시적 타입 선언
function getUserById(id: number): Promise<User | null> {
  return userRepository.findById(id);
}

// Avoid: 타입 생략
function getUserById(id) {
  return userRepository.findById(id);
}
```

### Interface vs Type

- **Interface**: 객체 형태, 확장 가능한 경우
- **Type**: Union, Intersection, 함수 타입 등

```typescript
// Interface 사용
interface User {
  id: number;
  name: string;
  email: string;
}

// Type 사용
type Status = 'pending' | 'approved' | 'rejected';
type CreateUserDto = Omit<User, 'id'>;
```

### Generic 활용

```typescript
// API 응답 타입
interface ApiResponse<T> {
  success: boolean;
  data: T;
  message?: string;
}

// Repository 패턴
interface Repository<T> {
  findById(id: number): Promise<T | null>;
  create(entity: Omit<T, 'id'>): Promise<T>;
  update(id: number, entity: Partial<T>): Promise<T>;
  delete(id: number): Promise<void>;
}
```

## 코딩 스타일

### 변수 선언

- `const` 우선, 재할당이 필요한 경우만 `let`
- `var` 사용 금지
- 의미 있는 변수명 사용

### 함수 정의

```typescript
// Arrow Function (권장)
const calculateTotal = (items: Item[]): number => {
  return items.reduce((sum, item) => sum + item.price, 0);
};

// Async Function
const fetchUser = async (id: number): Promise<User> => {
  const user = await userRepository.findById(id);
  if (!user) {
    throw new NotFoundException('User not found');
  }
  return user;
};
```

### 에러 처리

```typescript
// Custom Exception 사용
throw new BadRequestException('Invalid user data');
throw new NotFoundException('User not found');
throw new ConflictException('Email already exists');
```

## 타입 가드 활용

```typescript
// Type Guard 함수
function isString(value: unknown): value is string {
  return typeof value === 'string';
}

// 사용 예시
if (isString(data)) {
  // data는 string 타입으로 추론
  console.log(data.toUpperCase());
}
```
